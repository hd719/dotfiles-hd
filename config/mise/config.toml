[tools]
bun = "1.3.14"
# Keep the dedicated gopls directory before Go's bin directory on PATH. The
# shared bootstrap installs Go first because this backend uses `go install`.
"go:golang.org/x/tools/gopls" = "0.23.0"
go = "1.26.3"
node = "26.1.0"
pnpm = "11.2.2"
python = "3.14.5"
