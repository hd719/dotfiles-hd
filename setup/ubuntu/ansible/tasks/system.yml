---
- name: Create the workstation state directory
  ansible.builtin.file:
    path: /var/lib/dotfiles-hd
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Check whether the first-build upgrade is complete
  ansible.builtin.stat:
    path: /var/lib/dotfiles-hd/initial-upgrade-complete
  register: workstation_initial_upgrade

- name: Update Ubuntu packages for the first build
  ansible.builtin.apt:
    update_cache: true
    upgrade: dist
    cache_valid_time: 3600
  when: not workstation_initial_upgrade.stat.exists

- name: Record the completed first-build upgrade
  ansible.builtin.file:
    path: /var/lib/dotfiles-hd/initial-upgrade-complete
    state: touch
    owner: root
    group: root
    mode: "0644"
  when: not workstation_initial_upgrade.stat.exists

- name: Install the Brave repository key
  ansible.builtin.get_url:
    url: https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg
    dest: /usr/share/keyrings/brave-browser-archive-keyring.gpg
    owner: root
    group: root
    mode: "0644"

- name: Configure the Brave repository
  ansible.builtin.copy:
    dest: /etc/apt/sources.list.d/brave-browser-release.list
    owner: root
    group: root
    mode: "0644"
    content: |
      deb [signed-by=/usr/share/keyrings/brave-browser-archive-keyring.gpg] https://brave-browser-apt-release.s3.brave.com/ stable main

- name: Install the explicit workstation package set
  ansible.builtin.apt:
    name: "{{ workstation_apt_packages }}"
    state: present
    install_recommends: false
    update_cache: true

- name: Grow the existing root filesystem to the Vagrant disk
  ansible.builtin.script:
    cmd: files/grow-root-filesystem.sh
  register: grow_root_filesystem
  changed_when: "'root-filesystem-grown' in grow_root_filesystem.stdout"

- name: Enable workstation services
  ansible.builtin.systemd_service:
    name: "{{ item }}"
    enabled: true
    state: started
  loop:
    - avahi-daemon
    - docker
    - gdm3
    - open-vm-tools
    - ssh

- name: Boot into the graphical desktop by default
  ansible.builtin.command:
    cmd: systemctl set-default graphical.target
  register: graphical_target
  changed_when: "'Created symlink' in graphical_target.stderr"
